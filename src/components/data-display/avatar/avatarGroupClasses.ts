export interface AvatarGroupClasses {
  /** 루트 요소에 적용된 스타일. */
  root: string;
}

export type AvatarGroupClassKey = keyof AvatarGroupClasses;

export const avatarGroupClasses: AvatarGroupClasses = {
  root: '',
};
